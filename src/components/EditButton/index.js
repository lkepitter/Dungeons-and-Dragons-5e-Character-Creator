import React from "react";

const EditButton = () => {
  //make an input appear based on what character element we're editing
  return (
    <div>
      <button>Edit</button>
    </div>
  );
};

export default EditButton;
